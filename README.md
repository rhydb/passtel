# Prerequisites

`sqlc` is needed to compile Go code from the SQL.

# Setup

Install the Go dependencies:

```
go get
```

Compile the SQL using `sqlc`:

```
sqlc generate
```

Run the API:

```
go run main.go
```
